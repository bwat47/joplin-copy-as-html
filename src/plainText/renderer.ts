/**
 * Deprecated: former plain text renderer shim.
 *
 * This file is intentionally left blank. The plain text pipeline now uses the
 * token collector (`plainTextCollector.ts`) + formatter (`plainTextFormatter.ts`).
 */

export {};
